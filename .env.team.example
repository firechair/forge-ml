# Team MLflow and DVC Configuration Template
# Copy this file to .env.team and customize for your team

# MLflow Tracking Server
MLFLOW_TRACKING_URI=http://localhost:5000

# MinIO/S3 Configuration for MLflow Artifacts
MLFLOW_S3_ENDPOINT_URL=http://localhost:9000
AWS_ACCESS_KEY_ID=minioadmin
AWS_SECRET_ACCESS_KEY=minioadmin

# DVC Remote Configuration
# Option 1: MinIO (local S3-compatible)
# DVC_REMOTE_URL=s3://dvc-storage/data
# DVC_REMOTE_ENDPOINT=http://localhost:9000

# Option 2: AWS S3
# DVC_REMOTE_URL=s3://your-bucket/dvc-storage
# AWS_REGION=us-east-1

# Option 3: Google Cloud Storage
# DVC_REMOTE_URL=gs://your-bucket/dvc-storage

# Option 4: Azure Blob Storage
# DVC_REMOTE_URL=azure://your-container/dvc-storage

# Team Configuration
TEAM_NAME=your-team-name
PROJECT_NAME=your-project-name

# Usage:
# 1. Copy this file: cp .env.team.example .env.team
# 2. Edit .env.team with your team's values
# 3. Load in your shell: source .env.team
# 4. Or use with direnv: echo "source .env.team" > .envrc && direnv allow
